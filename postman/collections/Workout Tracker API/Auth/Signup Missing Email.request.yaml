$kind: http-request
url: "{{base_url}}/api/v1/auth/signup"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: text
  content: |-
    {
        "password": "TestPassword123",
        "display_name": "Test User"
    }
scripts:
  - type: afterResponse
    code: |-
      // Signup missing email test
      pm.test("Signup missing email returns 400 Bad Request", function () {
          pm.response.to.have.status(400);
      });

      pm.test("Response is JSON", function () {
          pm.response.to.be.withJson;
      });

      pm.test("Response contains error envelope", function () {
          var jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property('data', null);
          pm.expect(jsonData).to.have.property('error');
      });
    language: text/javascript
order: 1040