$kind: collection
description: Postman collection for Workout Tracker API containing health, debug, and authentication endpoints. Includes basic auth protected debug endpoints (expvar, statsviz) and JWT protected authentication endpoints (signup, login, logout).