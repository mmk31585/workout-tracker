name: Audit

on:
  push:
    branches: [main,develop]
  pull_request:
    branches: [main,develop]

jobs:
  audit:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: workout
          POSTGRES_USER: workout
          POSTGRES_PASSWORD: workout
        ports:
          - 5438:5432
        options: >-
          --health-cmd "pg_isready -U workout -d workout"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.27.0

      - name: Verify Depedencies
        run: go mod verify

      - name: Build
        run: go build -v ./...

      - name: Run go vet
        run: go vet ./...

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@latest

      - name: Run staticcheck
        run: staticcheck ./...

      - name: Run Tests
        env:
          DB_ADDR: postgres://workout:workout@localhost:5438/workout?sslmode=disable
        run: go test -race ./...
